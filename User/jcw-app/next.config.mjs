/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
    images: {
        domains: ['jhbchildwelfare.org.za','media.istockphoto.com','img.freepik.com'],
      },
    };

export default nextConfig;
